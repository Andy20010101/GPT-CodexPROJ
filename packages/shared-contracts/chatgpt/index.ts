/**
 * ChatGPT bridge contracts will live here.
 *
 * The service package owns the implementation. This package owns reusable
 * request and response shapes once the bridge API is wired in.
 */

export type PlaceholderBridgeContract = {
  readonly status: 'planned';
};
