# Risks And Assumptions

## Risks
- `<known project or environment risk>`
- `<known dependency or access risk>`

## Assumptions
- `<assumption about environment, repo, access, or dependency>`
- `<assumption that is currently acceptable but should remain visible>`

## Non-Blocking Unknowns
- `<unknown that does not currently block export>`
- `<unknown that should stay visible but does not change the frozen outcome>`
