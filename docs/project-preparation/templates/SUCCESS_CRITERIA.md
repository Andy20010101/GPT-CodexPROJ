# Success Criteria

## Success Definition
`<what must be true for this project to count as successful>`

## Required Evidence
- `<behavioral result evidence>`
- `<review, validation, or test evidence>`
- `<operator-visible evidence>`

## Failure Conditions
- `<what still counts as failure even if something ships>`
- `<what pseudo-completion should be rejected>`

## Notes
`<known limits that may be acceptable without changing the success definition>`
