# References

This directory is for documented reference material that helps explain or validate the platform.

- `legacy/` contains imported historical artifacts that are intentionally kept for context.
- References are read-only inputs, not active runtime surfaces.
- New ad hoc imports should not go here without a short note explaining why they are worth keeping.
